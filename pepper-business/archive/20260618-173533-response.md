# Documentation Execution — Tier-1 #1 done: "Writing Tests" guide

Branch: vex-na-update-docs (committed `eb69283cc`, pushed; MR 2220). Used the existing Coverage Gap Roadmap as source of truth; executed the top item.

## Gap Status (the 4 Tier-1 gaps)
| # | Gap | Status | Target path | Create now? |
|---|---|---|---|---|
| 1 | **How to write tests** | **MISSING → CREATED this turn** | `docs/onboarding/writing-tests.md` | ✅ done |
| 2 | **Business-rule ADRs** (deal-limit `0`=blocked/`1000`=unlimited; promotion eligibility = verified billing OR approved network) | **MISSING** | `docs/architecture/decisions/deal-submission-limit-model.md` + `…/promotion-eligibility.md` | next |
| 3 | **PII / data-handling + retention** | **MISSING** (only incidental substring mentions; no dedicated doc — the rigorous probe was zero) | `docs/architecture/data-handling-and-pii.md` (or a domains/ doc) | after #2 |
| 4 | **Invoice/reporting service boundaries** | **MISSING** | section in `docs/finance/` or `docs/architecture/` | last (largest) |

## Work Completed
- **Created `docs/onboarding/writing-tests.md`** — evidence-based from the harness:
  - The **two suites** (unit `tests/unit/<Area>` → `Tests\Unit\TestCase`; API `tests/api/group<N>` Cest) and where a new test goes.
  - **Isolation models clarified (corrects a common oversimplification):** unit tests isolate via a per-test `DB::beginTransaction()`/`rollBack()` (`tests/unit/TestCase.php:38,49`); the **API/Codeception** suite is `cleanup:false` + `disable_model_events:true` (`codeception.yml:22-23`) → no auto-rollback, model events don't fire.
  - **`DataManager` fixture catalog**, `SampleData`/`OcularEventAware`/`MetabaseMockData` traits.
  - **Event assertions** (`Event::fake()` + `assertOcularEventDispatchedForUser` + `isValid()` payload check).
  - **The deal-limit gotcha** (the practical unblocker): country defaults are `0` for DE/GB/FR/PL/NL (risk L1) → `store()` throws; grant `DataManager::addBusinessLimit($business,'DE',1000,1000,now,null)` to test the create path; inverse `0/0` to assert the gate (`StoreTest::testStoreWithLimitReached`).
  - **Why tests are skipped** ("Temporal Black Friday changes") + **how to re-enable** them.
  - Mocking Metabase for finance tests (M1); the `ReflectionMethod` private-method pattern.
- **Updated** `docs/onboarding/README.md` (read-order) and `docs/index.md` (onboarding ramp) to link it.
- QA: all 10 cross-link/file targets verified to resolve.
- *(Recovery note: the working tree was switched to `master` mid-turn by the environment; `writing-tests.md` was written there as untracked, then carried cleanly to `vex-na-update-docs` and committed alongside the hub — no loss, no orphaned file.)*

## Remaining Work (real, Tier-1)
- **#2 Business-rule ADRs** — deal-limit model + promotion eligibility (repo-derivable from `ThreadService`/`BusinessService`; small).
- **#3 PII / data-handling + retention** — what PII is stored where, block-not-delete & never-hard-delete semantics, deletion-request flow (mostly repo-derivable; policy is org).
- **#4 Invoice/reporting service boundaries** — the layering across `InvoiceService`/`GenerateInvoiceBaseService`/jobs and `Report`/`Performance`/`Metabase` (largest).
Tier-2/3 unchanged (see roadmap). Not repo-fixable: on-call/rollback (human), frontend (separate repo), validating M1/L1/GAM (external).

## Handoff Publication
Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated** `latest.md` → this execution turn.
- **Archived** the previous `latest.md` (coverage-gap roadmap) to `pepper-business/archive/` (timestamped) — no duplicates.
- **In-repo (committed `eb69283cc`, pushed to vex-na-update-docs / MR 2220):** **added** `docs/onboarding/writing-tests.md`; **updated** `docs/onboarding/README.md` + `docs/index.md`. No new top-level category; nothing merged/retired/deleted.

## Recommended Next Action

> ### 📐 Write the two Business-rule ADRs (Tier-1 #2)
> **Category:** Architecture/Business-Rules documentation (repository-only — derivable from `ThreadService::isDealLimitReached`/`BusinessService::checkIfLimitReached` and the promotion-eligibility checks; no external access).
> **Why next:** the writing-tests guide is shipped; the highest-ROI remaining item is encoding the two revenue-critical rules that are currently only **implicit invariants** — the **deal-limit model** (`0`=blocked, `1000`=unlimited, resolution order user→business→package→country default) and **promotion eligibility** (verified billing OR approved network). ADRs make them durable and **prevent L1-style recurrence** (the limit model already caused a 7-month regression). Small, self-contained, high durability.
> **Expected outcome:** `docs/architecture/decisions/deal-submission-limit-model.md` and `…/promotion-eligibility.md` (each: context, the rule, resolution order, what-must-not-break, evidence `file:line`).
> **Expected duration:** ~2–3h for both.
> **Confidence this beats stopping:** **High** — codifies revenue rules whose ambiguity has already cost real money; fully repo-doable.
