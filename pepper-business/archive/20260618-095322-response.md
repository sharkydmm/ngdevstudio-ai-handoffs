# F1 Secret-Exposure — Rigorous Validation (Investigation only)

Branch: vex-90-vue-migration-changes (work committed on vex-na-update-docs) · **No rotation, no removal, no infra change.** Values are **redacted** throughout — nothing secret is written to this handoff.

## Verdict up front
The prior "F1: critical exposure of real production credentials" was **partially correct but imprecise**. Rigorous validation shows a **mix**, not a blanket critical:
- The **most serious real exposure is a Google Ad Manager service-account private key** (`resources/google/keys.json`) — which the earlier pass did **not** identify.
- The **`.env.production` "prod creds" I flagged last turn are NOT exploitable** (they contain an AWS key **ID** and DB **host/username** only — **no AWS secret, no DB password**). That part was **overstated**.
- A **genuinely valid prod DB read credential** sits in a 2-day-old skill doc.
- The rest is **test/mock credentials** or **low-sensitivity** tokens.

## Findings — answers to the 10 questions

**1–3. What / where / introducing commits:**
| Item | File | Introduced | Type |
|---|---|---|---|
| **GAM service-account private key** (`private_key` 1704 chars, `client_email: pepperadmanager@pepper-270009…`) | `resources/google/keys.json` | `d82b0ce1b` **2020-03-12** (1 commit, unchanged) | **Real prod GCP key** |
| **Prod RDS read login** (host + user `majid.abdolhosseini` + plaintext password) | `docs/ai/skills/merchy-prod-db.md` | `6ed4637ca` **2026-06-15** | **Real prod DB cred** |
| AWS key **ID** + RDS host + DB **username** (NO secret, NO password) | `.env.production`, `.env.beta` | **2019-09-10** | Identifiers only |
| Test DB password (= README pw), MOCK S3 key/secret, test B2C consumer keys, `PEPPER_SIGNATURE_TOKEN`, `HMRC_CLIENT_SECRET` | `.env.tests` | 2021-03-16 | **Test/mock** |
| Schema-doc download token (`$secret`, gates `/schema` file download only) | `app/Http/Controllers/ApiController.php:58` | — | Low-sensitivity |
| Beta webhook secrets (mostly `md5(1)`/`md5(2)`/`md5(3)` placeholders, `betas.pepper.com`) | `config/pepperUriWebhooks.php` | — | Beta/placeholder |
| reCAPTCHA **site** key (public by design) | `.env.production` | — | Not secret |

**4–5. In HEAD or history-only?** All present in **HEAD** (current branch *and* master) — current, not history-only.

**6. Evidence they're production:** GAM key — `client_email=pepperadmanager@…`, is the app's default GAM key (`config/google-ads.php:50 jsonKeyFilePath => resource_path('google/'.env('AD_MANAGER_OAUTH2_KEY','keys.json'))`), and `.env.production` sets `AD_MANAGER_OAUTH2_KEY=keys…` → **prod points at this committed file**. DB skill — prod RDS cluster endpoint + `merchant_prod`. → both production. `.env.production` AWS_KEY is prod-labeled but only an ID.

**7. Still valid?** GAM key: **unverified but likely** (never rotated in 6+ yrs; still the default key file). DB cred: **likely** (2 days old, owner is an active committer). **Not testable from here** (no RDS egress) and intentionally not exercised.

**8. Systems affected if compromised:** Google Ad Manager (ad orders/lines/creatives → ad delivery & advertiser data) via the SA key; production MySQL (`merchant_prod` financial/PII/business data, **read**) via the DB cred. **AWS is NOT affected** — only the key ID is present, never the secret (verified: 0 populated `AWS_SECRET` lines ever in `.env.production`).

**9. Realistic blast radius:** **GAM key — High**: anyone with repo read access can auth to Google as the service account and manipulate/disrupt ad serving (revenue-critical) + read advertiser config. **DB cred — Conditional**: requires network reachability to the RDS (security-group dependent, unverified) and the actual grant (documented read-only, but the DB privilege is not verifiable from the doc). **Test/mock & low items — minimal.**

**10. Normal remediation:** rotate the GAM SA key (new key, update prod, revoke old) + audit GAM access logs; rotate the prod DB password + move the skill cred to a secret manager + audit DB access logs; remove all secret-bearing files from tracking, add to `.gitignore`, scrub history (git-filter-repo/BFG); rotate test creds as hygiene.

## Classification (the question asked)
- **Active security risk:** `resources/google/keys.json` (GAM key) — **and** `merchy-prod-db.md` (conditional on RDS reachability).
- **Historical security debt:** `.env.production`/`.env.beta` (IDs/hosts only, 2019; not exploitable).
- **Test/mock credentials:** `.env.tests`, README password.
- **Low-sensitivity / near-false-positive:** `ApiController` schema token; `pepperUriWebhooks` (beta, md5 placeholders); reCAPTCHA site key (public).
- **Already remediated:** none observed (GAM key unchanged 6 yrs; no rotation evidence).

## Confidence Assessment
| Claim | Confidence |
|---|---|
| Secrets present in HEAD (current) | **High** (git-verified) |
| GAM key is a real GCP private key & the app's prod key | **High** |
| GAM key is still *active* | **Medium-High** (no rotation in 6 yrs; default key) |
| Prod-DB cred is valid | **Medium** (recency + active user; untested) |
| `.env.production` is NOT exploitable (no secret/password) | **High** (verified absent) |
| DB exploitable externally today | **Low-Medium / Unknown** (RDS network exposure unverified) |

## Risks
- **GAM service-account key (High):** ad-serving manipulation/outage + advertiser data read = revenue-critical; 6-year exposure window.
- **Prod DB read cred (Medium-High):** read access to financial/PII data, *if* RDS is network-reachable and the grant is as broad as the login allows.
- **Process risk:** `.gitignore` excludes `.env`/`.env.testing` but not `.env.production`/`.beta`/`.tests` → secret files keep getting committed; the repo's own `AGENTS.md` rule is being violated.

## Recommendations (ranked by ROI)

**Top 3 highest-value engineering investigations**
1. **Confirm the GAM key is active** (GCP console: is `pepperadmanager@pepper-270009` key id still enabled?) → if yes, rotate. *Highest ROI — confirmed-real, revenue-impacting.*
2. **Confirm RDS network exposure + the `majid.abdolhosseini` grant** (is the SG internet-open? is the user truly read-only?) — determines the DB cred's true blast radius.
3. **Audit GAM + prod-DB access logs** for use from unexpected sources during the exposure window.

**Top 3 highest-value documentation tasks**
1. A **secret-inventory + rotation runbook** (this validated list, redacted; what to rotate, in what order).
2. Fix **`.gitignore`** guidance to cover `.env.*` and `resources/google/*.json` (doc + the actual ignore).
3. A short **"secrets must be env/secret-manager injected, never committed"** ADR (the repo already intends this but doesn't enforce it).

**Top 3 highest-risk blind spots**
1. Whether the GAM key has **already been used** by an unauthorized party (no log access here).
2. RDS **security-group** posture (internet-open vs VPC-only).
3. Whether `PEPPER_SIGNATURE_TOKEN` / B2C consumer keys in `.env.tests` are **shared with prod** (would upgrade them from test to active).

**What I would personally investigate next:** confirm the **GAM key's active status in GCP** — it's the single confirmed-real, revenue-impacting, longest-exposed credential; the answer directly decides whether an urgent rotation is warranted.

**What I would personally stop doing because ROI is now low:** treating `.env.production`/`.env.beta` as "leaked prod credentials" — validated as **identifiers only, not exploitable**; and authoring further narrative documentation. Redirect effort to confirming/rotating the two genuine items.

## Handoff Publication
Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated** `latest.md` → this rigorous F1 validation (supersedes the prior discovery's blanket F1 claim; **corrects** the `.env.production` overstatement; **adds** the GAM-key finding).
- **Retired/archived** the previous `latest.md` to `pepper-business/archive/` (timestamped) by the script — no duplicates.
- **No secret values** are included (redacted by design).

## Recommended Next Action

> ### 🔎 Confirm whether the exposed GAM service-account key is still active (then decide rotation)
> **Category:** Investigation → Runtime Validation (GCP/IAM), leading to Engineering Improvement.
> **Why highest ROI:** it is the only **confirmed-real, revenue-impacting** secret, exposed **6+ years**, and is the app's **default prod GAM key**. A 5-minute GCP check (is the `private_key_id` still enabled for `pepperadmanager@pepper-270009`?) decides between "urgent rotation" and "already-dead key = historical debt."
> **Expected duration:** ~10–15 min (GCP IAM check); rotation ~1h if active.
> **Expected outcome:** a definitive active/inactive determination for the GAM key; if active → rotate + audit GAM logs; if inactive → downgrade to historical debt and remove from repo.
> **Confidence this is the right next step:** **High** — highest validated severity, cheap to confirm, binary outcome.
