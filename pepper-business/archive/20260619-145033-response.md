# Spain (ES) invoicing exclusion — recorded as intentional business-rule exception (closed)

Branch: pp-na-adding-skills (committed `adaacbe59`, pushed). No ticket, per instruction.

## Recorded
Product-confirmed 2026-06-19: **Spain (ES) is intentionally excluded from ALL automatic invoice generation** (standard CPC/CPM *and* loyalty). Spain uses a **separate manual invoicing process** outside Pepper Business due to local business/accounting requirements. ES merchants still submit deals and keep their thresholds/config; **zero auto-generated invoices for ES is expected — not lost revenue, not an incomplete rollout, not a bug.**

Captured in:
- **`docs/reference/country-configuration.md`** (canonical) — replaced "skipped for standard generation" with the full business-rule exception + the *why* (manual process) + "do not treat as lost revenue"; noted both standard (`:608`) and loyalty (`:86`) skips.
- **`docs/finance/invoice-lifecycle.md`** — ES row updated to "ALL auto generation intentionally skipped — separate manual process."
- **`docs/ai/repository-memory.md`** — durable bullet: ES generates zero auto invoices by design.
- **`docs/documentation-audit/confidence-risk-register.md`** — round-2 question **Q2 marked ✅ RESOLVED (intentional, closed)**.

## Resolved-ambiguities ledger (running)
- **L1** — deal-limit `0/0` paywall → intentional (closed).
- **N1** — hardcoded year-specific BF config → intentional historical reference (closed).
- **N4** — `countriesWithNoIAPI` → intentional IAPI-exclusion mechanism (closed).
- **Loyalty "small business country" (PL/NL)** → intentional legal/tax requirement (closed).
- **Q2 / ES invoicing** → intentional manual-process exception (closed, this turn).

## Still-open business-logic questions (for a future turn)
Not asked now (you didn't request the next one): dead `disabled_invoice_reminders_countries` config (DE/GB/FR); DATEV for non-German markets (ES/NL/SE/US/PL); submit-time duplicate-deal check disabled; per-user deal limits disabled; loyalty market scope ({DE,NL,GB,FR,PL}); `pepper_api_mock` default. Say "next question" and I'll pick the highest-value one.

## QA / Handoff
No behaviour change (docs only). Handoff `latest.md` updated; previous archived (timestamped).
