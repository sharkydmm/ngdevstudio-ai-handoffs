# Reusable AI skill — Pepper Business MR review

Branch: pp-na-adding-skills (committed `943925067`, pushed; GitLab offers a new MR off it).

## Where it was created
`docs/ai/skills/review-merge-request.md` — the existing AI-skills location (alongside `add-network-provider.md`, `merchy-prod-db.md`), using the same YAML frontmatter (`name`/`description`) so Claude/Cursor can load it on demand. **No new category created.**

## What it contains (PB-specific, actionable)
- **Step 1 Load context:** VEX ticket → domain-map → canonical domain doc → service-catalog + ownership-map (who confirms intent) → risk register + ADRs.
- **Step 2 Identify domain/service** from diff paths (services/config/migrations/jobs/Ocular/Metabase).
- **Step 3 PB risk checklist** (a table, each row links the governing rule): deal-submission limits (`0`=blocked/`1000`=unlimited + resolution order), promotion eligibility (verified billing OR approved network), billing/invoice/reporting (Metabase source-of-truth, M1 column coupling), GAM/Ocular/Metabase (additive payloads, `(string)` typing, line-sync), thread lifecycle (status triple, publish-requires-accept, never-hard-delete), merchant/user lifecycle (block-not-delete, block cascade), country config (ES skip/PL/US-SE zero), limit resolution, events & queued jobs (Clicky single-worker invariant), hidden config (fail-open defaults/dead config), migrations & **no-DB-FK** data-shape risk, skipped/weak tests (Codeception gotchas + the `addBusinessLimit` fixture), call-site/signature.
- **Step 4 Classify** (correctness / revenue / reporting / operational / security-privacy / maintainability + severity).
- **Step 5 Evidence** in the house **verifiable-conclusion** format (claim + proof `file:line` + confidence + assumption + test/doc link).
- **Steps 6–8:** avoid low-value comments (don't flag ADR-intentional patterns), when to recommend tests, **when to STOP and ask product** (limit/config changes, eligibility/invoice math, Metabase card IDs, Ocular/API contracts, secrets).
- **Output template** (verdict table) + **good-vs-bad comment examples** + a links footer.
- QA: all 41 internal links resolve.

## Cross-linked from
- `AGENTS.md` AI Documentation Index (new row).
- `docs/ai/README.md` (AI docs table).
- `docs/index.md` (AI skills section).

## Handoff Publication
Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated** `latest.md` → this skill-creation turn.
- **Archived** the previous `latest.md` (final QA review) to `pepper-business/archive/` (timestamped) — no duplicates.
- **In-repo (committed `943925067`, pushed to pp-na-adding-skills):** **added** `docs/ai/skills/review-merge-request.md`; **updated** AGENTS, ai/README, index. Not yet merged (open as a fresh branch/MR; the docs corpus itself is already merged to `master` via MR 2220).

## Recommended next reusable skill
**Only one is genuinely worth it, and it's optional:** a **"validate a parked finding" skill** that operationalizes the M1/L1/GAM validation plans (the exact read-only Metabase/prod-DB/GCP checks) so that *when external access is available*, an agent can run them consistently. It would wrap the validation plans already in the risk register + the `merchy-prod-db` skill.

Otherwise, **I do not recommend more skills** — the review skill is the high-value one; further skills (incident-triage, add-country, release) would largely duplicate the production-support hub, the existing add-operating-country doc, and deployment docs. The remaining high-value work is **engineering execution** (validate L1/M1/GAM, rotate secrets, land the CI limit-test, re-enable Cluster-A tests), not more documentation or skills.
