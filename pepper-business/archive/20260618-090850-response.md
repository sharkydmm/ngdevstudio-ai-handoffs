# Pepper Business — Confidence & Risk Discovery (Investigation)

Branch: vex-na-update-docs · **Investigation only — no code/config/test changes.** Focus: discover unknowns, validate, increase confidence.

## Findings

### 🔴 NEW — F1. Committed production secrets in the repository (highest-severity discovery)
Tracked-in-git credential exposure (confirmed via `git ls-files` / `git grep`):
- **`.env.production`** is committed — contains a live **AWS access key ID** and the **production RDS host**. `.gitignore` ignores `.env`/`.env.testing`/`.env.local` but **not** `.env.production`.
- **`.env.beta`** and **`.env.tests`** are also committed (AWS key IDs + beta RDS host).
- **`docs/ai/skills/merchy-prod-db.md`** (commit `6ed4637ca`) embeds the **prod RDS host + username + plaintext password**.
- **`README.md`** contains a credential-looking password string.
- This **contradicts the project's own rule** (`AGENTS.md`: "Never read/echo/modify `.env*` … use `.env.example`") — yet prod/beta/test env files are in version control and in git history.
- *(Values are intentionally redacted here; they are visible in the repo. Anyone with repo read access — incl. history — has prod AWS + DB access.)*

### 🟠 NEW — F2. Knowledge-concentration / bus-factor risk on the core thread service
- **`ThreadService.php`**: **184 of 303 commits (61%) by Martin Kanchev — whose last commit anywhere was 2019‑01‑25** (last touched ThreadService 2018‑12‑05). The dominant author of the most central moderation/revenue service **left ~7 years ago**; original design intent is unowned.
- **Revenue source-of-truth concentrated in 2 active people:** Metabase 96% (Gospodinov+Genchev of 27 commits), Invoice 79%, AdManager 83%. Genchev (2026‑06‑17) and Gospodinov (2026‑06‑15) are active; Simeonov tapered (2024‑09). Loss of either materially slows safe changes to billing/reporting.

### 🟡 Runtime validation of the open finding — ATTEMPTED, BLOCKED here
- The sanctioned `merchy-prod-db` skill exists, but **this environment has no egress to the RDS** (TCP to `:3306` times out) and **no mysql client (cannot install — no sudo/network)**. So I could not run the prod query myself.
- **Handoff query for you to run** (read-only; resolves L1 definitively) — see Recommended Next Action.

### 🟢 Reconfirmed (prior passes) — still open
- **L1**: DE/GB/FR/PL/NL deal limits = `0` (AT in same BF batch restored to 5/5; these not). Likely incomplete BF revert. *Runtime check pending.*
- **L2**: `appConfig.pepper_api_mock` defaults to `true` (fail-open to mocked B2C if prod env unset; call-site `,false)` defaults are inert).
- **C1**: `disabled_invoice_reminders_countries` is dead config (no repo usage).

## Confidence Assessment

| Finding | Confidence | Basis |
|---|---|---|
| F1 committed secrets | **High** | `git ls-files` + `git grep` (provable from repo) |
| F2 bus-factor (Kanchev gone; 2-person revenue ownership) | **High** | `git log` authorship + last-commit dates |
| L1 0-limit is a regression | **Medium-High** | git diffs + AT-restored-but-others-not; **not yet runtime-confirmed** |
| L2 mock fail-open in prod | **Medium** | code proves default-true; prod `.env` unverifiable here |
| C1 dead config | **High** | repo-wide grep, zero usage |

## Risks

- **Security (F1):** live prod AWS + DB credentials in git → unauthorized prod access, data exfiltration, lateral movement. Severity: **Critical.**
- **Operational/maintenance (F2):** the core thread service's design owner is gone; revenue/reporting code held by 2 people → slow/risky changes, incident fragility.
- **Revenue (L1):** if `0` is a regression, businesses without explicit limits cannot submit deals in the 5 largest markets.
- **Data integrity (L2):** mocked B2C in prod (if env unset) → fake merchant/thread data silently.

## Recommendations (ranked by ROI)

**Top 3 highest-value engineering investigations**
1. **Rotate & purge the committed secrets (F1)** — rotate prod DB password, the AWS keys in `.env.production`/`.env.beta`/`.env.tests`, the README credential; remove env files from git + scrub history; move the prod-DB skill creds to a secret store. *Highest ROI — critical, concrete, time-boxed.*
2. **Runtime-confirm L1** via the prod query (below) — settles a live revenue question in minutes.
3. **Verify L2** (`PEPPER_API_MOCK=false` in prod) and flip the config default to fail-safe.

**Top 3 highest-value documentation tasks**
1. A short **secrets-handling + rotation runbook** (what was exposed, what to rotate, how secrets should be injected).
2. The **deal-submission limit-model ADR** (`0`=blocked, `1000`=unlimited) — still unwritten.
3. A **bus-factor / ownership map** (now evidence-based from git) to replace the undeclared-ownership gap.

**Top 3 highest-risk blind spots**
1. Whether the committed prod credentials have already been used/leaked (no runtime/log access here).
2. Whether DE/GB/FR/PL/NL `0` is currently blocking real revenue (runtime).
3. Prod `.env` truth for `PEPPER_API_MOCK` and other fail-open defaults.

**What I would personally investigate next:** **F1 secret rotation** — it is the only Critical-severity, fully-confirmed finding, and exposure compounds with time. Everything else is recoverable; leaked prod keys are not.

**What I would stop doing because ROI is now low:** **authoring net-new narrative documentation.** The corpus is mature and code-strong; further prose adds little confidence. Redirect to security remediation, runtime validation, and test re-enablement.

## Handoff Publication

Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated:** `latest.md` — now reflects this Confidence & Risk Discovery (F1 secrets, F2 bus-factor, runtime-blocked note, reconfirmed L1/L2/C1). Supersedes the prior config-audit `latest.md`.
- **Retired/archived:** the previous `latest.md` (config audit) is auto-archived to `pepper-business/archive/` by the handoff script (timestamped) — not duplicated.
- **Not added:** no secret values are written to the handoff (redacted by design).
- The repo now reflects the latest investigation state; prior findings remain in the dated archive entries.

## Recommended Next Action (pick this next)

> ### 🔐 Rotate & remove the committed production secrets
> **Category:** Engineering improvement (security remediation)
> **Why highest ROI:** the only **Critical, fully-confirmed** finding — live prod AWS keys + DB host/password are in git (and history). Exposure grows every day; rotation is cheap, the blast radius is the whole production system.
> **Expected duration:** ~1–2 hours (rotate creds) + history scrub (longer, can follow).
> **Expected outcome:** prod DB password + AWS keys rotated; `.env.production/.beta/.tests` removed from tracking and `.gitignore`’d; prod-DB skill creds moved out of the repo; a short rotation note recorded.
>
> **Runtime validation to run in parallel (read-only, via the merchy-prod-db skill):**
> ```sql
> -- Did DE/GB/FR/PL/NL deal submission stop after BF (2025-10-31) vs AT/ES/MX?
> SELECT country, DATE_FORMAT(created_at,'%Y-%m') AS ym, COUNT(*) AS deals
> FROM threads
> WHERE thread_type = 1 AND created_at >= '2025-08-01'
>   AND country IN ('DE','GB','FR','PL','NL','AT','ES','MX')
> GROUP BY country, ym ORDER BY country, ym;
>
> -- How often does the country default even apply? (businesses with explicit limits)
> SELECT country, COUNT(DISTINCT affected_id) AS businesses_with_limit
> FROM deal_limits WHERE affected_type LIKE '%Business' GROUP BY country ORDER BY country;
> ```
