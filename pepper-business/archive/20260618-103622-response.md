# GAM Service-Account Key — Active-vs-Historical Validation (Investigation only)

Branch: vex-90-vue-migration-changes (docs/handoff on vex-na-update-docs) · **No rotation, no infra change, no removal.** The private key material is never printed; only non-secret identifiers (needed for a GCP lookup) appear here.

## Verdict
The exposed GAM key is, on repo evidence, an **ACTIVE production credential**, not historical debt. The only link not verifiable from the repo is the **GCP-side enablement/roles** — that requires a console check (the recommended next action). Confidence it is active: **Medium-High**.

## Key identifiers (for the GCP/GAM check — not secrets)
- `client_email`: **pepperadmanager@pepper-270009.iam.gserviceaccount.com**
- `project_id`: **pepper-270009** · `client_id`: 109610161305594462298
- `private_key_id`: **fe4b59c3…** (check this id under that service account in GCP IAM)

## Findings — answers to the 10 questions

1. **Still active?** **Likely yes.** It is baked into the prod image, referenced by prod env, used by an actively-developed integration, and never rotated. (GCP enablement = the one unconfirmed link.)
2. **Still referenced by code/config/deployment/runtime?** **Yes, end-to-end:**
   - `config/google-ads.php:50` → `jsonKeyFilePath => resource_path('google/'.env('AD_MANAGER_OAUTH2_KEY','keys.json'))`
   - `.env.production` / `.env.beta` → `AD_MANAGER_OAUTH2_KEY="keys.json"` (points at the committed file)
   - `docker/prod.dockerfile:19` → `COPY . /var/www/html` (no `.dockerignore` → key baked into the image); `:61` → `cp .env.production .env` (committed prod env is the image base)
   - `app/Services/AdManager/BaseService.php` → builds the GAM session from that key and drives Order/LineItem/Creative/Inventory/PQL/Forecast services
3. **Evidence prod authenticates with it?** **Strong indirect.** Image-bake + env pointer + heavy active GAM use (VEX-297 AliExpress Jun 2026, VEX-102 GAM API upgrade, VEX-162 GAM Ocular, GAM SoapFault hotfix). No runtime-log access here to observe an actual auth, so not *direct*.
4. **Replacement credential?** **No evidence.** Single key file (`resources/google/keys.json`), single commit, no alternate key, no env override, no CI injection of a key. (A runtime volume-mount replacement could exist only in the **external** `kubernetes/*.yaml` deploy templates — not in this repo; see blind spots.)
5. **Already disabled?** **No evidence.** Never rotated in the repo (1 commit, 2020); GCP status unknown from here.
6. **Permissions/roles?** **Not determinable from the repo** — IAM roles and GAM-network user permissions live in GCP/GAM UI. Behaviourally, the app performs **create/update/pause/archive on orders & line items** and reads inventory/forecasts → the SA effectively holds **trafficker/write access** to the prod GAM network.
7. **Systems/workflows depending on it?** GAM ad serving: order/line/creative creation + status sync, the Dealsix→GAM flow, AliExpress targeting rule, B2C-thread-mark → line status sync, inventory/forecast. **Revenue-critical (billable ad delivery).**
8. **Impact if abused?** Create/modify/pause/archive ad orders & line items in the prod GAM network (ad-serving disruption or fraudulent delivery) + read advertiser/inventory data. **High, revenue-critical.**
9. **Impact if rotated?** Low-risk if coordinated: generate a new SA key, replace `resources/google/keys.json` (image/secret), redeploy, then revoke the old key. The file is read at GAM-session build time, so a redeploy with the new file is sufficient. Done carelessly (revoke before deploy) → GAM order creation/sync breaks until the new key propagates.
10. **Confidence in the assessment?** **Medium-High** overall — repo chain is strong and consistent; the residual unknown is purely GCP-side (enablement + exact roles + any external volume-mount override).

## Timeline
- **Introduction:** `d82b0ce1b` **2020-03-12** (Svilen Simeonov) — key committed.
- **First usage:** GAM integration wired around then (`config/google-ads.php`, AdManager services).
- **Current usage:** actively used through **2026** (multiple GAM commits incl. Jun 2026); baked into every prod image via `COPY .`.
- **Replacement:** none found in the repo.
- **Current status:** present in HEAD on all branches; **never rotated**; presumed active.

## Isolated or broader pattern?
**Broader, systemic credential-management pattern** — not isolated. The repo commits credential-bearing files (`.env.production/.beta/.tests`, the prod-DB skill doc, this GAM key) instead of externalising them; the prod Dockerfile even relies on the committed `.env.production` (`cp .env.production .env`); `.gitignore` covers only `.env`/`.env.testing`. The GAM key is the most severe instance of this pattern. (Secrets injected at runtime via the K8s `pepper-business-api-env` secret are env vars only — they do **not** override the committed key file, on available evidence.)

## Confidence Assessment
| Claim | Confidence |
|---|---|
| Key is referenced by prod config/deploy/runtime | **High** (code + dockerfile + env) |
| Committed key is the credential prod uses | **Medium-High** (image-baked; no override evidence; residual: external deploy templates) |
| Key still active in GCP | **Medium-High** (never rotated + active integration; GCP unconfirmed) |
| Effective write/trafficker capability to GAM | **Medium-High** (behavioural: app does order/line CRUD) |
| Part of a broader secrets-in-repo pattern | **High** |

## Risks
- **GAM key (High, revenue-critical):** repo access → Google auth as the SA → manipulate prod ad delivery + read advertiser data. 6-year exposure window.
- **Systemic (Medium):** the commit-secrets pattern means rotation alone won't fix the class; new secrets will keep landing in git.

## Recommendations (ranked by ROI)

**Top 3 highest-value engineering investigations**
1. **GCP IAM check** for `pepperadmanager@pepper-270009` / key id `fe4b59c3…`: is the key enabled? what IAM roles? what GAM-network user permissions? → definitive active/inactive + true blast radius. *Highest ROI, ~10 min, binary.*
2. **Inspect the external `kubernetes/*.yaml` deploy templates** for any volumeMount/secret overriding `resources/google/keys.json` — the only scenario where the committed key is NOT the live one.
3. **GAM + GCP audit-log review** for the SA's recent activity / use from unexpected sources during the exposure window.

**Top 3 highest-value documentation tasks**
1. A **secret-inventory + rotation runbook** (this key incl. `private_key_id`, the prod-DB cred, `.env.*`) — order of operations to rotate safely.
2. **`.gitignore` + `.dockerignore` hardening** (cover `.env.*`, `resources/google/*.json`) with a short rationale note.
3. A **"secrets via secret-manager, never `COPY .`/committed"** ADR — encode the intent the repo currently violates.

**Top 3 highest-risk blind spots**
1. GCP-side key status/roles (only place the "active" question is fully answerable).
2. External deploy-template volume mounts (could override the key — unseen here).
3. Whether the SA key has already been used by an unauthorized party.

**What I would personally investigate next:** the **GCP IAM/GAM console check** — it's the single unresolved link and it converts "Medium-High, likely active" into a definitive yes/no that decides urgent rotation.

**What I would personally stop doing because ROI is now low:** trying to determine the key's *live* status from the repo — repo evidence is exhausted; the remaining truth is only in GCP. Also stop re-litigating `.env.production` as a leaked secret (validated: identifiers only).

## Handoff Publication
Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated** `latest.md` → this GAM-key active-vs-historical validation (supersedes the prior F1-validation latest; adds the deployment-chain proof + timeline + identifiers for the GCP check).
- **Retired/archived** the previous `latest.md` to `pepper-business/archive/` (timestamped) by the script — no duplicates.
- **No private key material** included (only non-secret identifiers needed for the lookup).

## Recommended Next Action

> ### 🔑 GCP IAM / GAM console check on the exposed service account
> **Category:** Runtime Validation (GCP/IAM + GAM admin).
> **Why highest ROI:** every repo-side link is now confirmed (baked into image, prod env points at it, active GAM use, never rotated). The **only** thing standing between "Medium-High likely-active" and a definitive call is the GCP side — and it's a ~10-minute lookup with a binary outcome that directly decides whether an urgent rotation is needed.
> **What to check:** in project **pepper-270009**, is key `private_key_id fe4b59c3…` for **pepperadmanager@pepper-270009.iam.gserviceaccount.com** still enabled? What IAM roles does the SA hold, and what user-level permissions does it have on the GAM network?
> **Expected duration:** ~10–15 min.
> **Expected outcome:** definitive active/inactive + role scope → if active, urgent coordinated rotation + GAM audit-log review; if disabled, downgrade to historical debt and de-track from the repo.
> **Confidence this is the right next step:** **High.**
