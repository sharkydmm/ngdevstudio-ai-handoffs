# Confidence & Risk Discovery — L1 RESOLVED: the 2025 Black-Friday limit reset was never rolled back

Branch: vex-90-vue-migration-changes (docs/handoff on vex-na-update-docs) · **Investigation only — no code/config changes.** GAM investigation remains **paused** at the GCP boundary (no new repo evidence appeared).

## Headline finding (NEW, repo-proven)
The long-standing open finding L1 ("DE/GB/FR/PL/NL = 0 deal limits — regression or intent?") is now **CONFIRMED a regression, with the exact culprit commit and a 2-year pattern proving it.**

`git log -L` on DE's `max_deals_per_week` line shows **three Black-Friday cycles**, the first two reverted, the third not:

| BF cycle | "set to BF" commit | Rolled back? |
|---|---|---|
| 2023 | PP-2678 parts 1–10 (Nov 2023) → DE 1000→2→0 | ✅ **Yes** — PP-2702 `a660862a8` (2023-11-28) restored DE→1000 |
| 2024 | `06fe704a7` (2024-10-31) → DE 1000→1 | ✅ **Yes** — `869ed57cd` (2024-12-02) restored DE→1000 |
| **2025** | **`842d7ec58` (2025-10-31, "0 default deal submission limits for DE, UK, FR, PL, AT, NL")** → DE 1000→**0** | ❌ **NO** — never rolled back; DE is **still 0** (~7.5 months) |

- **DE's most recent limit change is the 2025 BF commit itself** — there is no later restore. Same for FR (1000→0) and PL (3→0).
- **AT was restored to 5/5** after the 2025 BF, but **DE/GB/FR/PL/NL were not** → a *partial* cleanup that missed the five. (GB/NL also have a murkier 2025 sub-history but are 0 now.)
- The **same commit (`842d7ec58`, same date) also skipped the Cluster A thread/deal/campaign tests** ("Temporal Black Friday changes") — so **both the config and its tests have been stuck in Black-Friday mode since 2025-10-31.**

## Answers to the 5 questions
1. **Highest-value unresolved unknown?** It *was* L1 — now resolved to **High confidence** (regression). The remaining open piece is its **live impact magnitude** (how many DE businesses actually rely on the country default vs have explicit limits) — needs the prod query.
2. **Most reduces business risk?** Restoring the missed 2025 BF revert for DE/GB/FR/PL/NL (after the impact query) — directly un-blocks deal submission in the biggest markets.
3. **Most reduces operational risk?** A **post-BF rollback checklist / dated revert task** — the root cause is a *process* gap (manual annual BF limit changes with a manual revert that was forgotten once in three years).
4. **Most improves reporting confidence?** Auditing the **Metabase card-ID map** (revenue/report source-of-truth) — not yet examined.
5. **Most improves revenue-critical confidence?** The **prod deal-submission-by-country query** + documenting the limit model.

## Confidence Assessment
| Claim | Confidence |
|---|---|
| L1 is a regression (2025 BF reset never reverted) | **High** — `git log -L` line-history is unambiguous |
| DE/FR/PL were non-zero (1000/1000/3) pre-2025-BF | **High** — verified at `842d7ec58^` |
| Pattern: 2023 & 2024 BF were reverted, 2025 was not | **High** — three cycles in the line-history |
| `0` blocks submission when the country default applies | **High** (prior pass: `posted >= max`) |
| **Live revenue impact magnitude** (how many businesses blocked) | **Low / Unknown** — needs prod data (businesses with explicit limits are unaffected) |

## Risks
- **Revenue (High likelihood, Unknown magnitude):** since 2025-10-31, DE/GB/FR/PL/NL businesses **without** an explicit business/package limit cannot submit deals (country default = 0). DE/FR are the largest markets. Magnitude depends on override prevalence (prod query).
- **Process (Medium):** BF limit changes are manual and the revert is manual — it was missed once already; will recur without a checklist/automation.
- **Test confidence (Medium):** Cluster A tests remain dark for the same reason; they can't be re-enabled until the limit model is settled.

## Recommendations (ranked by ROI)

**Top 3 highest-value engineering investigations**
1. **Prod query: deal submissions by country since 2025-10-31 + businesses relying on country default** → quantifies the regression's real revenue impact (urgent vs benign). *~15 min with DB access; binary-ish outcome; High value.*
2. **Audit the Metabase card-ID map** (`config('metabase.*')`) for stale/duplicate/missing card IDs → reporting/revenue source-of-truth correctness (next-biggest unexamined unknown).
3. **(Paused) GAM GCP/IAM check** — unchanged; resume when GCP access is available.

**Top 3 highest-value documentation tasks**
1. **Deal-submission limit-model + BF-rollback runbook** (now fully evidenced): `0`=blocked, `1000`=unlimited, the 3-cycle history, and a dated "revert BF limits" checklist to prevent recurrence.
2. The **secret-inventory + rotation runbook** (GAM key incl. `private_key_id`, prod-DB cred, `.env.*`).
3. **`.gitignore`/`.dockerignore` hardening** note (cover `.env.*`, `resources/google/*.json`).

**Top 3 highest-risk blind spots**
1. Live revenue impact of L1 (prod data).
2. Metabase card-ID correctness (reporting feeds invoicing).
3. GAM key live status (GCP — paused).

**What I would personally investigate next:** the **prod deal-submission-by-country query** — L1 is now a *confirmed* regression; the only thing left is *how much it's costing*, which decides whether product treats it as urgent (restore defaults now) or benign (most businesses have explicit limits).

**What I would personally stop doing because ROI is now low:** further **git-archaeology on L1** — it's conclusively resolved; additional repo digging yields nothing. Also stop re-examining `.env.production` and continue treating GAM as paused.

## Handoff Publication
Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated** `latest.md` → this L1-resolution (supersedes the GAM-validation latest; GAM noted as paused). Adds the 3-cycle line-history proof + culprit commit `842d7ec58`.
- **Archived** the previous `latest.md` (GAM validation) to `pepper-business/archive/` (timestamped) by the script — no duplicates.
- **Retired:** the "L1 = suspected/unresolved" framing from earlier handoffs is superseded by "L1 = confirmed regression."

## Recommended Next Action

> ### 📊 Quantify the L1 regression with a prod query (deal submissions by country since 2025-10-31)
> **Category:** Runtime Validation (read-only prod DB).
> **Why highest ROI:** L1 is now a **confirmed** regression (culprit `842d7ec58`, 2025-10-31, never reverted — breaking the 2023/2024 pattern). The only missing input is **magnitude**: are real DE/GB/FR/PL/NL businesses blocked, or do they all have explicit limits that bypass the country default? That single number decides whether this is an urgent revenue fix or benign debt.
> **Queries (read-only, via the merchy-prod-db skill — needs DB egress the repo lacks):**
> ```sql
> SELECT country, DATE_FORMAT(created_at,'%Y-%m') ym, COUNT(*) deals
> FROM threads WHERE thread_type=1 AND created_at>='2025-08-01'
>   AND country IN ('DE','GB','FR','PL','NL','AT','ES','MX')
> GROUP BY country, ym ORDER BY country, ym;          -- did DE/etc. drop to ~0 after 2025-10?
>
> SELECT country, COUNT(DISTINCT affected_id) businesses_with_limit
> FROM deal_limits WHERE affected_type LIKE '%Business' GROUP BY country;  -- how many bypass the default
> ```
> **Expected duration:** ~15 min (with DB access).
> **Expected outcome:** a definitive "X businesses / Y deals blocked since Oct 2025" → urgency call + a concrete config-restore recommendation for product.
> **Confidence this is the right next step:** **High** — the regression is proven; impact is the only open variable.
