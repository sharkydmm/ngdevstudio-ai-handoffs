# Staff-Engineer pick — inline deal-limit guardrail at the edit site (L1 recurrence prevention)

Branch: vex-na-update-docs (committed `301db4d24`, pushed; MR 2220). Repository-only, correct-by-construction.

## Chosen Task
Add an **inert invariant comment** to `config/countries.php` (the exact file where the L1 regression occurred) warning that `0` = **BLOCKED**, `1000` = unlimited, `0` is only for non-deal markets (US/SE), and that lowering a deal market to `0` for a campaign **must be reverted**.

## Why It Has The Highest ROI
- **Targets the confirmed, highest-cost regression (L1):** a ~7-month revenue block caused by a forgotten Black-Friday revert. The guardrail sits **where the mistake is made** — anyone editing `max_deals_per_*` sees the invariant + the "schedule the revert" warning before they commit.
- **Real risk reduction, not documentation:** it's an engineering guardrail at the code edit site (not a doc in `docs/`), and it operationalizes the ADR exactly where it's actionable.
- **Zero-risk / correct-by-construction:** a PHP block comment is inert; `php -l` confirms no syntax errors and all 11 country blocks/values are unchanged. I deliberately avoided test/build changes because **I can't run the suite or Docker build in this sandbox** — an unverifiable failing test would be a net negative; a comment cannot break anything.
- **5-minute, repo-only, fully completable + verifiable here** (re-read + lint), unlike the parked items (need prod-DB/GCP/Metabase) and unlike secret rotation (needs external rotation + history rewrite).

## Work Completed
- `config/countries.php` — added the `⚠ DEAL-SUBMISSION LIMIT INVARIANT` header comment (links `deal-submission-limit-model` ADR + risk register L1). Values untouched; `php -l` clean. Committed `301db4d24`, pushed to MR 2220.

## Remaining Risks
- **Soft guardrail:** a comment can be ignored. The *hard* version is a **CI test** (assert `0`=blocked / non-zero allows at the unit level, and/or a config-structure test) — but that needs a test run I can't perform here, so it's the natural follow-up, not this task.
- **L1 itself is unfixed:** the comment prevents *recurrence*, it does not restore DE/GB/FR/PL/NL — that still needs the prod-impact query + a product-signed config restore.
- Parked items (L1 impact, GAM key, M1) unchanged — all behind external access.

## Recommendations (ROI-ranked, repo-only first)
1. **(soft→hard) Add a CI guardrail test** locking the limit semantics (`checkIfLimitReached`/`userLimits`: `0`→blocked, non-zero→allowed) — needs a test run; pairs with this comment.
2. **Re-enable one skipped Cluster-A happy-path test** via the documented `addBusinessLimit` fixture (needs a test run to confirm).
3. Non-repo (higher absolute value, external): validate L1 impact (prod query) → restore config; rotate the committed GAM key + prod-DB cred; validate M1.

## Handoff Publication
- **Updated** `latest.md` → this guardrail turn.
- **Archived** the previous `latest.md` (Tier-1 #4 closure) to `pepper-business/archive/` (timestamped) — no duplicates.
- **In-repo (committed `301db4d24`, pushed to vex-na-update-docs / MR 2220):** modified `config/countries.php` (comment only). Nothing else added/merged/retired.

## Recommended Next Action

> ### ✅ Add the CI test that locks the deal-limit semantics (the "hard" guardrail)
> **Category:** Test improvement (engineering) — repository-only authoring, but **requires a test run to verify** (Docker `business-app-host`), which this sandbox can't do.
> **Why:** converts the just-added soft comment + the ADR into a CI-enforced invariant — a future refactor changing `>=` to `>` (flipping `0` from blocked to allow-all) would then fail CI. Pairs naturally with the guardrail just shipped.
> **If a test environment isn't available:** the highest-value step is **non-documentation and external** — run the read-only **L1 impact query** (prod DB via the `merchy-prod-db` skill) to size the regression and decide the config restore. I can classify the output if pasted.
>
> *(The Tier-1 documentation initiative remains complete; this and the above are execution/guardrail work, not new docs.)*
