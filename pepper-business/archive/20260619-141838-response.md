# L1 closed (intentional paywall) + business-logic ambiguity sweep

Branch: pp-na-adding-skills (committed `4f5430e6c`, pushed). Docs only; no behaviour change; `php -l` clean.

## L1 closed as intentional
Product (Martin) confirmed: `0/0` deal-limit defaults for selected markets (e.g. DE/UK) are **expected commercial paywall behaviour** — submission is tied to an active **package** or an explicit **business-level limit**, country default is the fallback, and `0` deliberately blocks unpaid/unconfigured businesses. **Not a regression.** Updated:
- **Risk register L1** → **✅ Closed (intentional)**; table-row status updated; the **lesson preserved**: *this was a documentation/knowledge ambiguity, not a code bug; future agents must ask product before treating a commercial paywall / blocked-by-config flow as a regression.*
- **deal-limit ADR** → "Open question" replaced with "Resolved — intentional paywall (product-confirmed)" + the lesson.
- **`config/countries.php`** comment → "confirmed intentional paywall; product sign-off to change."
- **validate-parked-findings skill** → L1 marked **CLOSED**; obsolete L1 queries removed (M1/S1 remain).
- **repository-memory** → new durable invariant: *`0/0` defaults are an intentional paywall; ask product before classifying paywalls as bugs.*
- Softened the residual "regression" wording in AGENTS index, index, ownership-map, backlog, CODEOWNERS.recommended.

## Business-logic ambiguity sweep (new — questions for product)
Added an **"Open business-logic questions (for product)"** section to the [risk register](../documentation-audit/confidence-risk-register.md). Not investigated deeply, **not assumed to be defects** — each surfaces a repo observation that *looks* like a bug but may be intentional:

1. **Paywalled-vs-open market split** (Revenue) — DE/GB/FR/PL/NL = `0` (paywall) but **AT/ES/MX = 5/5** (open). *Q: is the split intentional — AT/ES/MX deliberately open while the others are package-gated?*
2. **ES invoicing skipped** (Finance) — `shouldSkipInvoiceGeneration()` hardcodes `['ES']` → Spain generates no invoices. *Q: is Spain intentionally not invoiced, or should ES be invoiced?*
3. **Dead reminder config** (Finance/Comms) — `disabled_invoice_reminders_countries=['DE','GB','FR']` is referenced nowhere. *Q: should reminders be suppressed for DE/GB/FR (enforced elsewhere?), or is this config obsolete?*
4. **DATEV for non-German markets** (Finance/Accounting) — `accounting_app=DATEV` for ES/NL/SE/US/PL. *Q: are these correctly mapped to DATEV, or should any use a local accounting app?*
5. **Submit-time duplicate-deal check disabled** (Threads/Moderation) — exists but TODO-gated off. *Q: intentionally off, and should it be re-enabled?*
6. **User-level deal limits disabled** (Threads/Limits) — polymorphic on User but "Disabled functionality". *Q: intentionally business/package-only, or planned to return?*
7. **`pepper_api_mock` default `true`** (Integrations/Ops; eng-hardening) — prod sets false, but a missing env would mock B2C. *Q: flip the default to `false` (fail-safe)?*

Each row has: suspected ambiguity · why it looks suspicious · why it may be intentional · domain · the exact question. As answered → document-as-policy or open a ticket; **don't change behaviour on assumption.**

## QA
`php -l config/countries.php` clean (no behaviour change); links in the edited docs resolve (8/4/11/5, 0 broken).

## Handoff Publication
- **Updated** `latest.md` → this turn.
- **Archived** the previous `latest.md` (deal-limit correction) to `pepper-business/archive/` (timestamped).
- **In-repo (committed `4f5430e6c`, pushed to pp-na-adding-skills):** closed L1 across register/ADR/skill/config/repository-memory + softened one-liners; added the business-logic question list. No new docs; nothing retired.

## Business-logic questions for Martin/product (concise)
1. Is the **paywalled (DE/GB/FR/PL/NL) vs open (AT/ES/MX)** deal-submission split intentional per-market policy?
2. Is **Spain (ES) intentionally excluded from invoice generation**?
3. Should **invoice reminders be suppressed for DE/GB/FR** — and is the dead `disabled_invoice_reminders_countries` config obsolete or should it be wired up?
4. Are **ES/NL/SE/US/PL correctly on DATEV** accounting, or should any use a local app?
5. Is the **submit-time duplicate-deal check intentionally disabled**, and should it be re-enabled?
6. Are **per-user deal limits intentionally disabled** (business/package-level only)?
7. Should **`pepper_api_mock`'s default be flipped to `false`** so a missing env can't mock B2C in production?
