# Tier-1 #4 done — Invoice & Reporting boundaries · Documentation Initiative Closure Review

Branch: vex-na-update-docs (committed `43bd6c33c`, pushed; MR 2220). **Tier-1 documentation set is now COMPLETE.**

## Work Completed
**Created** `docs/finance/invoice-and-reporting-boundaries.md` — maps:
- **Invoice generation layers:** `invoices:generate` (command) → `GenerateInvoiceBaseService` (pulls Metabase, groups by business+billing+country, `:88-100` dispatches) → `ProcessInvoiceReports` (job, per-group) → `CreateInvoiceDB`/`CreateLoyaltyInvoiceDB` → persist + `SendOcularEvent(InvoiceData)` + `RunningInvoicesGeneratedDueServiceTerminationNotify` + numbering/PDF/email. The **generation-vs-CRUD** boundary (`InvoiceService`/`RunningInvoiceService` are read/serve, not generate).
- **Reporting layers:** `PerformanceReportService` and `ReportService` → `Metabase/*` per-card consumers → `MetabaseApiClient` (cards by ID, column display-name coupling).
- The **shared Metabase dependency (M1)** and an observed coupling: the test `MetabaseMockData` trait is `use`d in three production services (flagged for cleanup).

**Updated** `docs/finance/README.md`, `docs/index.md` (finance section), `docs/architecture/service-catalog.md` (overlap finding → "now documented"), `docs/documentation-audit/documentation-improvement-backlog.md` (Tier-1 #4 ✅ → **set complete**). QA: all 12 targets resolve.

---

# Documentation Initiative Closure Review

### 1. What areas are now sufficiently documented?
Threads (lifecycle/state/troubleshooting), Merchant & User lifecycles, the **data-model atlas**, **service catalog**, **ownership map**, **risk register**, Ocular catalog, Clicky, GAM, Pepper B2C, the finance/invoicing flow **+ now the invoice/reporting boundaries**, production-support (structure + runbooks), API endpoints, the full onboarding ramp **+ writing-tests**, and the business-rule ADRs (deal-limit model, promotion eligibility) **+ data-handling/PII**. These need **maintenance, not expansion**.

### 2. What documentation work should stop because ROI is low?
- **New top-level docs/categories and narrative prose** — the structural + Tier-1 layers are complete.
- **Re-deriving the parked findings** (M1/L1/GAM) — preserved in the register; only external validation adds value.
- **Documenting org/on-call/rollback without a human** — only stubs are possible.

### 3. What meaningful documentation gaps still remain?
Only **Tier-2/3** (nice-to-have): external-consumer/webhook contracts, translations/i18n workflow, a secrets-handling ADR + `.gitignore`/`.dockerignore` hardening, and the legacy-doc "status" banners. None are high-ROI.

### 4. Which remaining gaps require…
- **External access:** validating **M1** (Metabase), **L1** impact (prod DB), **GAM key** status (GCP).
- **Human knowledge:** on-call/alerting/rollback/deploy-approval/access provisioning; legal **retention periods** + the **DSAR/erasure process** owner; team **ownership** confirmation for CODEOWNERS.
- **Another repository:** frontend (`pepperbusinessview`) flows; the `kubernetes/*.yaml` deploy templates (CI-include repo).

### 5. Highest-value remaining activities that are NOT documentation (ranked by ROI)
1. **Validate L1 impact** (read-only prod query) — confirmed regression; size it → fix-or-benign decision. *Cheap, revenue-critical.*
2. **Rotate/secure the committed GAM key + prod-DB cred** (S1/S2) — confirmed exposure; rotation + `.gitignore` hardening.
3. **Validate M1** (Metabase card→column diff) — protects the revenue source-of-truth.
4. **Re-enable the skipped Cluster-A tests** using the documented `addBusinessLimit` fixture — converts code-only revenue/moderation paths to test-verified.
5. **Add `CODEOWNERS` + 2nd reviewers** for orphan areas (B1 bus-factor) — needs handles confirmed.

## Handoff Publication
Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated** `latest.md` → this Tier-1 #4 + closure review.
- **Archived** the previous `latest.md` (Tier-1 #3 PII) to `pepper-business/archive/` (timestamped) — no duplicates.
- **In-repo (committed `43bd6c33c`, pushed to vex-na-update-docs / MR 2220):** **added** the boundary doc; **updated** finance README, index, service-catalog, backlog. No new category; nothing merged/retired/deleted.

## Recommended Next Action

> **Tier-1 documentation initiative is complete. Remaining high-value work is not documentation.**
>
> The single highest-ROI next step is **validating the L1 deal-limit regression with a read-only production query** (Runtime Validation) — it sizes a confirmed revenue regression in ~15 minutes and decides whether an urgent config restore is needed. It requires prod-DB access not available from this environment (run via the `merchy-prod-db` skill, or paste the output and I'll classify it).
>
> If you'd rather stay in-repo, the only remaining doc work is **Tier-2 polish** (legacy-doc banners, secrets-handling ADR + `.gitignore` hardening) — genuinely low ROI; I'd recommend pausing documentation here.
