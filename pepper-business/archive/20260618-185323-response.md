# Documentation Execution — Tier-1 #3 done: Data handling & PII

Branch: vex-na-update-docs (committed `04fdc51f5`, pushed; MR 2220). Repository-only; code refs with `file:line`.

## Work Completed
**Created**
- `docs/architecture/data-handling-and-pii.md`

**Updated**
- `docs/architecture/README.md` — added to "Related architecture docs" (also added service-catalog, which was missing there).
- `docs/index.md` — linked under Permissions & auth.
- `docs/documentation-audit/documentation-improvement-backlog.md` — Tier-1 #3 marked **✅ DONE**; #4 marked **NEXT (last remaining Tier-1)**.

QA: all 11 cross-doc/code targets resolve.

## Doc Summary
Evidence-derived PII/data-handling reference. Key content:
- **PII inventory** (entity → fields → table): `users` (firstname/lastname/email/password-hashed/tokens/timezone), `user_invitations` (invitee email), `users_terms` (consent), `businesses` (invoice_email/name), `invoices` (legal_business_name/address/zip/city/email), billing owners/emails, free-text comments/threads.
- **Transmission boundaries:** invoice PDFs/emails; Ocular carries **identifiers, not names/emails**; Metabase may surface legal names; B2C/GAM/Salesforce/accounting cross the boundary.
- **The reality: deactivate, don't erase.** No `SoftDeletes` anywhere; no `->delete()`/`destroy()` path in `UserService`/`BusinessService`; users **blocked** (`UserStatus::BLOCKED=0`), threads **never hard-deleted**. Only **token tables are purged on schedule** (`expiredTokens:delete` 02:00, `expiredInvoiceTokens:delete`, `clicky:…:purge` 04:30). Users/businesses/invoices/threads are **retained indefinitely** (no code-enforced retention window).
- **Erasure gap:** no anonymize/“forget”/export mechanism exists; a subject-erasure request has **no automated path**, and manual deletion is risky because referential integrity is **app-level, not DB-enforced** (won't cascade — ties to data-model D1).
- Engineer guidance (don't add hard-delete; keep PII out of Ocular payloads; register new PII fields) and **⚠ POLICY** items (legal retention periods, DSAR/erasure process owner) explicitly flagged as org-owned, not in repo.

## Remaining Tier-1 Work
- **#4 Invoice/reporting service boundaries** — the last remaining Tier-1 item (the layering across `InvoiceService`/`GenerateInvoiceBaseService`/`Invoice/*` jobs and `Report`/`Performance`/`Metabase`). Largest of the four.
After #4, Tier-1 is **exhausted**. Tier-2/3 unchanged. Not repo-fixable: on-call/rollback (human), frontend (separate repo), validating M1/L1/GAM (external).

## Handoff Publication
Published to `github.com/sharkydmm/ngdevstudio-ai-handoffs` → `pepper-business/`:
- **Updated** `latest.md` → this Tier-1 #3 turn.
- **Archived** the previous `latest.md` (Tier-1 #2 ADRs) to `pepper-business/archive/` (timestamped) — no duplicates.
- **In-repo (committed `04fdc51f5`, pushed to vex-na-update-docs / MR 2220):** **added** the PII doc; **updated** architecture README, index, backlog. No new top-level category; nothing merged/retired/deleted.

## Recommended Next Action

> ### 🧩 Write the Invoice + reporting service-boundary doc (Tier-1 #4 — last remaining Tier-1)
> **Category:** Finance/Architecture documentation (repository-only — derivable from `app/Services/Invoice/*`, `app/Jobs/Invoices/*`, `app/Services/Metabase/*`, `ReportService`/`PerformanceReportService`; no external access).
> **Why next:** it's the **last remaining Tier-1 gap** and the **highest-confusion revenue area** the service catalog flagged — invoice logic is split across a root service, a base service, and queued jobs (post VEX-77), and reporting spans three layers. A "which layer owns what + command→service→job→event→Metabase flow" doc removes real ambiguity, aids money-path incident response, and reinforces the M1 risk context.
> **Expected outcome:** a boundary/flow doc (section in `docs/finance/` or `docs/architecture/`) mapping the invoice + reporting layers, their handoffs, and owners.
> **Expected duration:** ~1–1.5h.
> **Confidence this beats stopping:** **High** — finishes the Tier-1 set; repo-doable; targets the money path.
>
> *After #4, the Tier-1 list is complete — the genuinely higher-value remaining work (validating M1/L1/GAM, bus-factor/secrets remediation) is **not documentation** and needs external access or a human decision.*
